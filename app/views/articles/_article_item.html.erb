<li>
  <%= link_to article_path(article), class: "flex flex-row mb-2 border rounded" do %>
    <div class="select-none cursor-pointer rounded-md flex flex-1 items-center p-4 transition duration-500 ease-in-out transform hover:-translate-y-1 hover:shadow-lg">
      <div class="flex flex-col rounded-md w-10 h-10 justify-center mr-4">
        <%= image_tag 'unicorn.svg' %>
      </div>
      <div class="flex-1 pl-1 mr-16 text-left">
        <div class="font-medium"><%= article.title %></div>
        <div class="text-gray-600 text-sm"><%= sanitize short_description_content(article.content) %></div>
      </div>
      <div class="animate-pulse text-gray-600 text-xs"><%= time_ago_in_words(article.created_at) %> ago</div>
    </div>
  <% end %>
</li>
